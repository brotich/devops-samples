version: "2.3"

services:
  react-client:
    build:
      context: client
      dockerfile: ../docker/Dockerfile.client
      target: development
    image: "react-client-demo:development"
    volumes:
      - ./client:/client/app
    ports:
      - "3000:3000"
